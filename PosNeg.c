#include <stdio.h>
/*
void main()
{
    int num; //declare num variable as integer type

    printf("Enter a number: \n"); //\n moves to next line
    scanf_s("%d", &num); 
    if (num > 0)
        printf("%d is a positive number \n", num);
    else if (num < 0)
        printf("%d is a negative number \n", num);
    else
        printf("0 is neither positive nor negative");
}
*/